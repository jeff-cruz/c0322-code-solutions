/* exported truncate */
/* function truncate(length, string) {
  for (var i = 0; i < string.length; i++) {
    var newString = '';

    if (string[i] < length) {
      var character = string[i];
      var ellipsis = '...';

    }
  }
  return phrase;
} */
