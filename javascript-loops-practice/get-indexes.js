/* exported getIndexes */
function getIndexes(array) {
  var newArray = [];
  for (var i = 0; i < array.length; i++) {
    newArray.push(i);
  }
  return newArray;
}

/*
1. create new array
2. access each index of the array
3. push value of each index of array into new array
4. return new array
*/
