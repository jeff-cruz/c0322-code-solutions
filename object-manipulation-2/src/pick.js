/* exported pick */
// function pick(source, keys) {
//   var newObject = {};
//   var entries = Object.entries(source);
//   var properties = Object.keys(source);
//   var values = Object.values(source);

//   console.log(keys);

//   for (var key in source) {
//     fpr
//     if (keys[i] !== key) {
//     }
//   }
// console.log(newObject);
// }
//   var newObject = {};
//   var entries = Object.entries(source);
//   console.log(entries);
//   for (var key in source) {
//     for (var i = 0; i <= keys.length; i++) {
//       if (keys[i] in source) {
//         newObject[key] = entries[i][1];
//       }
//       delete newObject[key];
//     }
//   }
//   return newObject;
// }

// create empty object assigned to object
// for every key in source
// if keys includes the property in the source
// create the property in object with the value of property in source
